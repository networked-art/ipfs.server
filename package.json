{
  "name": "@1001/ipfs-server",
  "version": "1.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "upload": "set -a && . ./.env.production && set +a && node scripts/upload.ts",
    "kamal:setup": "set -a && . ./.env.production && set +a && kamal _2.10.1_ setup",
    "kamal:deploy": "set -a && . ./.env.production && set +a && kamal _2.10.1_ deploy",
    "kamal:deploy:ipfs1": "set -a && . ./.env.production.ipfs1 && set +a && kamal _2.10.1_ deploy",
    "kamal:sh": "set -a && . ./.env.production && set +a && kamal _2.10.1_ app exec -i --reuse \"sh\"",
    "changeset": "changeset"
  },
  "packageManager": "pnpm@10.30.0",
  "devDependencies": {
    "@changesets/changelog-github": "^0.6.0",
    "@changesets/cli": "^2.30.0"
  },
  "dependencies": {
    "kubo-rpc-client": "^6.1.0"
  }
}
