# Required locally but deliberately untracked. Source .env.production before
# deploying so Kamal obtains registry credentials, then export the opaque
# RAINBOW_ORIGIN_TOKEN separately or store it in the local environment.
KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD
RAINBOW_ORIGIN_TOKEN=$RAINBOW_ORIGIN_TOKEN
